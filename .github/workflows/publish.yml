name: publish
'on':
  push:
    branches:
      - ktp-pydata-versions

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: install dependencies
      run: |
        source doc/set_version.sh
        echo $DOC_VERSION